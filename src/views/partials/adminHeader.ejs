<header class="bg-gray-800 text-white p-4">
  <div class="container mx-auto">
    <div class="flex justify-between items-center">
      <h1 class="text-2xl font-semibold">MoviesFlix Admin</h1>
      <div class="flex items-center">
        <span class="mr-4">Bienvenido, <%= user.name %></span>
        <form action="/admin/logout" method="GET" class="mb-0">
          <button type="submit" class="bg-transparent border-2 border-white text-white text-sm px-4 py-2 rounded-md hover:bg-white hover:text-gray-800 transition duration-200">Cerrar Sesión</button>
        </form>
      </div>
    </div>
  </div>
</header>
